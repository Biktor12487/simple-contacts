import React from 'react';

import { ContactsList } from '../../containers';
import { MainLayout } from '../../components';

const Contacts = () => (<MainLayout><ContactsList /></MainLayout>);

export default Contacts;
